"use client"; import { useState } from 'react'; import { supabase } from '../lib/supabase'; import { useAuth } from '../contexts/AuthContext'; import FileUpload from './FileUpload'; import { NewFolderModal } from './NewFolderModal'; export default function FolderView() { return ( <div className="p-4"> <h2 className="text-xl font-bold">Pastas</h2> <FileUpload folderId="123" disciplineName="Matemática" onUploadSuccess={() => {}} /> <NewFolderModal onClose={() => {}} onSuccess={() => {}} /> </div> ); }